# Claude Code Project Memory

Import the shared repository rules from `AGENTS.md`.

@AGENTS.md

## Claude-specific guidance

- Use `.claude/agents/*` for specialized role reviews and implementations.
- Use `.claude/skills/*` for repeatable workflows.
- Use `.claude/rules/*` for path-scoped guidance.
- Reviewer agents should not implement unless explicitly asked.
- Implementer agents should make one narrow change and request review.
